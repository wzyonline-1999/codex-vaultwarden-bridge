# Contributing

Thanks for helping improve Codex Vaultwarden Bridge.

Before opening a pull request:

1. Keep secret handling conservative.
2. Add tests for any new extraction, redaction, or command execution behavior.
3. Run:

```bash
npm run verify
```

Never commit real secrets, private keys, exported vaults, or session tokens.

